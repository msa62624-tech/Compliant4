# Backend Environment Configuration

# Server
PORT=3001
NODE_ENV=development

# JWT
JWT_SECRET=your-secret-key-change-in-production

# CORS - Set to your frontend URL in production
FRONTEND_URL=http://localhost:5175

# For production with compliant.team domain:
# FRONTEND_URL=https://compliant.team
# OR for other deployments:
# FRONTEND_URL=https://i-nsuretrack1234.vercel.app

# SMTP (Email) - configure one of the following

# Option A: Microsoft 365 / Outlook (recommended)
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=your.email@compliant.team
SMTP_PASS=your-password
SMTP_FROM=your.email@compliant.team
SMTP_SECURE=false
SMTP_REQUIRE_TLS=true
SMTP_TLS_REJECT_UNAUTHORIZED=true

# Option B: Gmail (via App Password)
# 1) Enable 2-Step Verification on your Google Account
# 2) Create an App Password for "Mail" → Name it "InsureTrack"
# 3) Use your full Gmail address as SMTP_USER and the 16-char App Password as SMTP_PASS
# SMTP_SERVICE=gmail
# SMTP_USER=your.email@gmail.com
# SMTP_PASS=your-16-char-app-password
# SMTP_FROM=your.email@gmail.com
# SMTP_REQUIRE_TLS=true
# Optional: override secure mode; leave unset to auto
# SMTP_SECURE=false

# Option C: Custom SMTP host
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-username
# SMTP_PASS=your-smtp-password
# SMTP_FROM=no-reply@compliant.team
# SMTP_SECURE=false
# SMTP_REQUIRE_TLS=true
# SMTP_TLS_REJECT_UNAUTHORIZED=true
# Admin emails (comma-separated for multiple admins)
# These emails receive COI review notifications and compliance alerts
ADMIN_EMAILS=admin@compliant.team,compliance@compliant.team