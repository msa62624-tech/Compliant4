### Backend environment example (do NOT commit real secrets)
# Copy this file to .env and fill values locally or in your deployment settings

# ====================
# SERVER CONFIGURATION
# ====================
PORT=3001
NODE_ENV=development

# Frontend URL used to generate links in emails (no trailing slash)
# Example: https://app.insuretrack.com or http://localhost:5175
FRONTEND_URL=http://localhost:5175

# ====================
# SECURITY
# ====================
# JWT secret - REQUIRED in production, must be at least 32 characters
# Generate a secure random string: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-me-in-production

# Admin password hash - REQUIRED in production
# Generate: node -e "const bcrypt = require('bcryptjs'); console.log(bcrypt.hashSync('YourSecurePassword', 10));"
ADMIN_PASSWORD_HASH=

# Default GC password - Used when seeding initial GC account for empty database
# REQUIRED in production, has insecure fallback in development
# Use a strong, random password that is at least 12 characters long
# For production, use a password manager or generate with: openssl rand -base64 24
DEFAULT_GC_PASSWORD=

# ====================
# OPERATIONAL
# ====================
# Graceful shutdown timeout (milliseconds)
SHUTDOWN_TIMEOUT=30000

# ====================
# EMAIL CONFIGURATION
# ====================
# Admin emails (comma separated) - receives notifications and alerts
ADMIN_EMAILS=admin@insuretrack.com

# SMTP / Email settings - do NOT commit real credentials. Use environment variables
# Examples for Office365 / Microsoft 365
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=your-email@domain.com
SMTP_PASS=your-smtp-password
SMTP_FROM=your-email@domain.com

# Optional SMTP flags
SMTP_SERVICE=
SMTP_SECURE=false
SMTP_REQUIRE_TLS=true
SMTP_TLS_REJECT_UNAUTHORIZED=true

# Adobe PDF Services (Optional - for PDF extraction and analysis)
# Sign up at https://developer.adobe.com/document-services/
# Create a project and obtain credentials
# If not configured, the system will use mock PDF extraction data
# ADOBE_API_KEY=your-adobe-api-key-here
# ADOBE_CLIENT_ID=your-adobe-client-id-here
