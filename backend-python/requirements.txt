# FastAPI - Modern Python web framework (equivalent to Express.js)
fastapi==0.115.6  # Updated from 0.109.0 - fixes ReDoS vulnerability
uvicorn[standard]==0.27.0

# Authentication & Security
python-jose[cryptography]==3.4.0  # JWT tokens - Updated to fix ECDSA vulnerability
passlib[bcrypt]==1.7.4  # Password hashing (equivalent to bcryptjs)
python-multipart==0.0.22  # Updated from 0.0.6 - fixes multiple vulnerabilities (ReDoS, DoS, arbitrary file write)

# Database (PostgreSQL support)
psycopg2-binary==2.9.9
sqlalchemy==2.0.25

# Email
aiosmtplib==3.0.0  # Async SMTP (equivalent to nodemailer)
jinja2==3.1.3  # Email templates

# PDF handling
PyPDF2==3.0.1  # PDF parsing (equivalent to pdf-parse)
reportlab==4.0.9  # PDF generation (equivalent to pdfkit)

# Security & Middleware
python-dotenv==1.0.0  # Environment variables
pydantic==2.5.3  # Data validation
pydantic-settings==2.1.0  # Settings management

# Rate limiting
slowapi==0.1.9  # Rate limiting middleware

# Monitoring & Logging
prometheus-client==0.19.0  # Metrics (equivalent to prom-client)

# CORS
# (built into FastAPI)

# Utilities
python-dateutil==2.8.2
validators==0.22.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0  # HTTP client for testing

# System metrics
psutil==5.9.8  # System and process utilities

# Optional integrations (Adobe PDF, AI)
openai==1.10.0  # AI integration
requests==2.31.0  # HTTP requests for external APIs
